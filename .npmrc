# React Native's Metro and CocoaPods autolinking assume a flat, hoisted
# node_modules. pnpm's default isolated store breaks native resolution, so we
# opt into a hoisted layout for this workspace.
node-linker=hoisted
shamefully-hoist=true
